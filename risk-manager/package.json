{
  "scripts": {
    "start": "node dist/server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "prepare": "tsc"
  },
  "devDependencies": {
    "@types/jest": "^27.0.0",
    "@types/supertest": "^2.0.0",
    "jest": "^27.0.0",
    "supertest": "^6.0.0",
    "ts-jest": "^27.0.0"
  }
}