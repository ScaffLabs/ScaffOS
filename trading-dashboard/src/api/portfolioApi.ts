import { Request, Response } from 'express'; import { InMemoryStore } from '../storage/InMemoryStore'; import { Position, validatePosition } from '../types'; import { ServiceError, NotFoundError, ValidationError } from '../utils/errors'; import logger from '../utils/logger'; import { query } from '../utils/connectionPool'; const positionStore = new InMemoryStore<Position>(); export const fetchPositions = async (req: Request, res: Response) => { try { const { limit = 10, offset = 0, sortBy = 'symbol', sortOrder = 'asc', filter = '' } = req.query; const sql = 'SELECT * FROM positions WHERE symbol LIKE ? ORDER BY ?? ?? LIMIT ? OFFSET ?'; const positions = await query(sql, [`%${filter}%`, sortBy, sortOrder, Number(limit), Number(offset)]); res.status(200).json(positions); } catch (error) { logger.error('Error fetching positions', { error: error.message }); res.status(500).json({ message: 'Error fetching positions' }); } }; export const createPosition = async (req: Request, res: Response) => { const positionData = req.body; try { validatePosition(positionData); const sql = 'INSERT INTO positions (id, symbol, quantity) VALUES (?, ?, ?)'; await query(sql, [positionData.id, positionData.symbol, positionData.quantity]); logger.info('Position created', { positionId: positionData.id }); res.status(201).json({ message: 'Position created successfully', position: positionData }); } catch (error) { if (error instanceof ValidationError) { logger.warn('Validation error in createPosition', { message: error.message }); return res.status(400).json({ message: error.message }); } logger.error('Error creating position', { error: error.message }); res.status(500).json({ message: 'Error creating position' }); } }; export const updatePosition = async (req: Request, res: Response) => { const { id } = req.params; const positionData = req.body; try { validatePosition(positionData); const sql = 'UPDATE positions SET quantity = ? WHERE id = ?'; const result = await query(sql, [positionData.quantity, id]); if (result.affectedRows === 0) throw new NotFoundError('Position not found'); logger.info('Position updated', { positionId: id }); res.status(204).send(); } catch (error) { if (error instanceof NotFoundError) { logger.warn('Position not found', { positionId: id }); return res.status(404).json({ message: error.message }); } if (error instanceof ValidationError) { logger.warn('Validation error in updatePosition', { message: error.message }); return res.status(400).json({ message: error.message }); } logger.error('Error updating position', { error: error.message }); res.status(500).json({ message: 'Error updating position' }); } }; export const deletePosition = async (req: Request, res: Response) => { const { id } = req.params; try { const sql = 'DELETE FROM positions WHERE id = ?'; const result = await query(sql, [id]); if (result.affectedRows === 0) throw new NotFoundError('Position not found'); logger.info('Position deleted', { positionId: id }); res.status(204).send(); } catch (error) { if (error instanceof NotFoundError) { logger.warn('Position not found', { positionId: id }); return res.status(404).json({ message: error.message }); } logger.error('Error deleting position', { error: error.message }); res.status(500).json({ message: 'Error deleting position' }); } }; export const registerRoutes = (app: any) => { app.get('/api/positions', fetchPositions); app.post('/api/positions', createPosition); app.put('/api/positions/:id', updatePosition); app.delete('/api/positions/:id', deletePosition); };