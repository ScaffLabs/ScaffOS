version: '3.8'
services:
  trading-dashboard:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    depends_on:
      - db
  db:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: trading_dashboard
    ports:
      - '3306:3306'