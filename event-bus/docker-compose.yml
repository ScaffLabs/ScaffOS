version: '3.8'
services:
  event-bus:
    build: .
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      REDIS_URL: redis://redis:6379
  redis:
    image: 'redis:alpine'
    ports:
      - '6379:6379'
